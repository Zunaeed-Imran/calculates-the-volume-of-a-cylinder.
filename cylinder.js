let height = prompt("Enter the Height of Cylinder: ");
let radius = prompt("Enter the radius of Cylinder: ");

let result = Math.PI * Math.pow(radius, 2) * height;

document.write("The area of Cylinder is " + result);